package com.example.adbanmap.map.repository;

import com.example.adbanmap.map.entity.MapDescription;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MapDescriptionRepository extends JpaRepository<MapDescription, Long> {
}
